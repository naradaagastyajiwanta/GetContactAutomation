server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    # Cache static assets
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Docker embedded DNS — re-resolve on every request so stale IPs after container
    # restarts don't cause 502s. valid=5s means re-query every 5 seconds.
    resolver 127.0.0.11 valid=5s;
    set $upstream_orchestrator http://orchestrator:8000;

    # API proxy → orchestrator
    location /api/ {
        client_max_body_size 50m;
        client_body_buffer_size 50m;
        rewrite ^/api/(.*) /$1 break;
        proxy_pass $upstream_orchestrator;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
        proxy_connect_timeout 10s;
    }

    # WebSocket proxy
    location /ws {
        proxy_pass $upstream_orchestrator/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 86400s;
    }

    # Webhook passthrough
    location /webhook/ {
        proxy_pass $upstream_orchestrator/webhook/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # SPA fallback — all routes → index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
